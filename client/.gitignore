# client/.gitignore
# Minimal — root .gitignore already covers node_modules, dist, IDE, OS, logs.
# This file exists because Vite scaffolding creates it per sub-project,
# which is standard practice for monorepo / nested package setups.

# Vite local env overrides (not needed at root level)
*.local
